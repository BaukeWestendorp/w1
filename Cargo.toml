[package]
name = "w1"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "w1"
path = "src/main.rs"
required-features = ["cli"]

[features]
cli = ["dep:clap"]

[dependencies]
# feature = "cli"
clap = { version = "4.6.6", features = ["derive"], optional = true }
